
# This file tells Netlify how to handle your React Router routes
[build]
  publish = "dist"
  command = "npm run build"

# This redirect rule tells Netlify to redirect all routes to index.html
# which allows React Router to handle the routing client-side
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
